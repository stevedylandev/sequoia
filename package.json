{
	"name": "sequoia-monorepo",
	"private": true,
	"workspaces": ["packages/*", "docs"],
	"scripts": {
		"build": "bun run --filter '*' build",
		"dev": "bun run --filter '*' dev"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"typescript": "^5"
	}
}
