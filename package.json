{
	"name": "sequoia-monorepo",
	"private": true,
	"workspaces": [
		"packages/*",
		"docs"
	],
	"scripts": {
		"format:cli": "cd packages/cli && bun run format",
		"format:server": "cd packages/server && bun run format",
		"format:docs": "cd docs && bun run format",
		"dev:cli": "cd packages/cli && bun run dev",
		"dev:docs": "cd docs && bun run dev",
		"build:docs": "cd docs && bun run build",
		"build:cli": "cd packages/cli && bun run build",
		"deploy:docs": "cd docs && bun run deploy",
		"deploy:cli": "cd packages/cli && bun run deploy",
		"dev:server": "cd packages/server && bun run dev",
		"start:server": "cd packages/server && bun run start"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"typescript": "^5"
	}
}
